# Layer 2 Serving API base URL.
# Leave empty in dev (Vite proxies /api and /ops to localhost:8000).
# In production set to the deployed Layer 2 host, e.g. https://engine.mallory.internal
VITE_API_BASE_URL=
